# Sourced by rcm (rcup, rcdn, mkrc, lsrc) as a shell script.

DOTFILES_DIRS="${HOME}/Workspaces/github.com/kdisneur/dotfiles";

case "$(uname -s)" in
  Darwin) TAGS="macos";;
  Linux)  TAGS="linux";;
esac

EXCLUDES="README.md";

# Machine-specific additions live in an untracked ~/.rcrc.local, e.g. at work:
#   DOTFILES_DIRS="${DOTFILES_DIRS} ${HOME}/path/to/work-dotfiles";
#   TAGS="${TAGS} work";
if [ -r "${HOME}/.rcrc.local" ]; then
  . "${HOME}/.rcrc.local";
fi
