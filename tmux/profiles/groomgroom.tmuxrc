# vim: syntax=tmux

set-option default-command "cd ${HOME}/Workspace/fewlines/groomgroom_web; exec ${SHELL}"

rename-window code
set-buffer "vim\n"
paste-buffer
splitw -v -p 20 'exec $SHELL -l'

neww -n servers
set-buffer "mix phoenix.server\n"
paste-buffer

selectw -t code
