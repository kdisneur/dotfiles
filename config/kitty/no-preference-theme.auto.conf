# vim:ft=kitty

# GNOME reports the colour scheme as "no-preference" rather than "light"
# whenever Dark style is simply not enabled, so this file has to carry the
# light palette or the desktop default would fall back to kitty's own colours.
include light-theme.auto.conf
