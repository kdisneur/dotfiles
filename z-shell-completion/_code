#compdef code

typeset -A opt_args

_arguments -C '1:profile:->profiles' && ret=0
case "$state" in
  profiles)
    local profiles; profiles=(arena_apps autr)
    _describe -t profiles 'profiles' profiles && ret=0
    ;;
esac

return 1
