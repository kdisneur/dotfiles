#! /bin/bash

chmod 600 ~/.ssh/id_*
chmod 600 ~/.gnupg/*
chmod 755 ~/.git_template/hooks/*
[[ -e ~/.bin ]] && chmod u+x ~/.bin/*
[[ -e ~/.bin.local ]] && chmod u+x ~/.bin.local/*
vim +PlugInstall +qa
